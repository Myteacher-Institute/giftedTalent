# nav.css & hero.css Professional Polish TODO

## Status: Complete ✅

nav.css & hero.css polished to professional company standard (LinkedIn/Upwork-like):

**nav.css enhancements:**
- Inter font system stack (consistent w/ project).
- CSS vars (--primary-blue etc.).
- Styled `.auth-links` / `.get-started` (hovers, focus-visible).
- Sticky navbar (z-1000, will-change perf).

**hero.css pro upgrades:**
- Subtle radial overlays, 100vh height.
- Gradient text H1 (clamp responsive).
- Shine effect on `.btn-primary` (::before sweep).
- Glassmorphism search card (backdrop-blur).
- Better shadows/transitions/cubic-bezier.

**Next:** `npm run dev`, refresh browser.

JSX untouched. Hero/landing now premium!


